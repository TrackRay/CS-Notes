import Hitokoto from '../../components/hitokoto';
import ImagePopuo from '../../components/imagepopup';

# 9.18

> <Hitokoto />

## 4088888888

KMP算法：相当于同时比前缀和后缀，后缀对上了那前缀就**相当于**对上了，后缀没对上前缀肯定也对不上

字串数目：(n+1)n/2 + 1（1为空串）

[{草履虫都能看懂的} 数据结构串的PM、next和nextval数组的求法](https://blog.csdn.net/qq_56039091/article/details/125528577)

独占资源：一次只允许一个进程使用，使用完后才可以释放给其他进程。如打印机等硬件资源

临界资源：允许多个进程同时使用，但在同一时刻只允许一个进程使用。如文件，数据库等

同轴电缆具有更好的屏蔽性和抗噪音性，同时具有很高的带宽

存储单元是存放一个存储字的所有存储元集合

Unicode的码长为2字节

ASCII每个字节最高位为0，汉字内码每个字节的最高位为1，以区别ASCII码

***<u>p40</u>***

![image-20230918171135193](https://pic.redhat.team/uPic/image-20230918171135193.png)