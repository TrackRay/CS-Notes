import Hitokoto from '../../components/hitokoto';
import ImagePopuo from '../../components/imagepopup';

# 9.21

> <Hitokoto />

## 4088888888

若有n个叶子结点，则一棵哈夫曼树的结点总数为2n-1

树结构中，结点总数（包括根和叶子）=  边数 + 1

每个进程拥有一个页表，且页表常驻内存中

后退n帧协议发送窗口大小：WT ≤ 2^m-1（m为帧序号位数）

**循环冗余编码**是检错码，因此采用**自动重发**机制；海明码检测并纠正一位错误代码的纠错码

**原码加减交替除法**

将被除数 - 除数
结果大于0，商1，余数左移一位
结果小于0，商0，恢复余数，余数左移一位
重复上述操作，直至商的精度满足要求为止
故小于0时，恢复余数

![image-20230921092910525](https://pic.redhat.team/uPic/image-20230921092910525.png)

取指令需要1次访存（不少考生会忽略），然后取两个操作数两次访存，保存运算结果1次访存，一共需要4次访存

一个指令m步，每一步一个时钟周期，一共n个指令 

- 非流水线；m*n*T

- 标量流水线 mT+(n-1)T

RISC计算机以硬布线逻辑为主，不用或者很少使用微程序控制

指令定长、对齐、仅Load/Store指令访存，以上三个都是**RISC**的特征。均能够有效的**简化流水线的复杂度**

有三种相关可能引起指令流水线阻塞：1.结构相关，又称资源相关；2.数据相关；3.控制相关
主要由转移指令引起。数据旁路技术，其主要思想是不必待某条指令的执行结果送回到寄存器，再从寄存器中取出该结果，作为下一条指令的源操作数，而是直接将执行结果送到其他指令所需要的地方，这样可以使流水线不发生停顿

**寄存器是否可见**

![image-20230921154824715](https://pic.redhat.team/uPic/image-20230921154824715.png)

**树的度**指的是树中**最大的结点度**

**指令完成访存**次数，注意：不要忘记**取指令**的一次访存

**SMTP**用于**用户代理向邮件服务器发送**邮件，或在**邮件服务器之间发送**邮件。SMTP**只支持传输 7 比特的ASCII码**内容